#include <iostream>
#include <vector>
#include <typeinfo>
#include <string>

using namespace std;

int main() {
    vector<int> adj = {1, 2, 3, 4, 5};
    string hello = "hello";
    cout << typeid(1).name() << endl;
    for (int i = 0; i < 5; ++i) {
        cout << hello.replace << endl;
    }
    return 0;
}